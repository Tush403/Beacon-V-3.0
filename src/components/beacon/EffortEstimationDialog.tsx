'use client';

// This component is no longer used and has been replaced by an inline result display.
// It is kept here to avoid breaking imports but will be removed in a future update.
export function EffortEstimationDialog() {
  return null;
}
